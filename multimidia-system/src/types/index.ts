// Tipos para links
export type SocialLink = {
  name: string;
  icon: string;
  url: string;
  color: string;
};

// Tipo para items de acesso rápido
export type QuickLinkItem = {
  title: string;
  icon: string;
  color: string;
  path: string;
};